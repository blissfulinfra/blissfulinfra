apiVersion: v1
kind: Service
metadata:
  name: {{SERVICE_NAME}}-stable
  labels:
    app: {{SERVICE_NAME}}
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  # Argo Rollouts injects rollouts-pod-template-hash into this selector to
  # pin the Service to the stable ReplicaSet.
  selector:
    app: {{SERVICE_NAME}}
